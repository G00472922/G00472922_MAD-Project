# Setup

In order for the application to run, a local environment file (named <b>environment.local.ts</b>) must be created in the environments folder.

Within this file, the following exported variable containing your API key must be added in:

```sh
## environment.local.ts

export const API_KEY = 'YOUR_API_KEY';
```
